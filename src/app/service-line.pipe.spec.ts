import { ServiceLinePipe } from './service-line.pipe';

describe('ServiceLinePipe', () => {
  it('create an instance', () => {
    const pipe = new ServiceLinePipe();
    expect(pipe).toBeTruthy();
  });
});
